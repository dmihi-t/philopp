

<%= form_with model: [@title, @comment], url:liberty_title_comments_path, local: true do |f| %>
  <div class="form-input">
    <%= f.text_area :comment, class: 'form-message', placeholder: 'type a message' %>
  </div>
  <%= f.submit '我思う故に我送信', class: 'form-submit' %>
<% end %>

<%# html: {class: "form"},  %>